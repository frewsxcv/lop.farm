language: python
python: 3.5
sudo: required
services:
  - docker
install:
  - docker build -t lop.io .
before_script:
  - pip install flake8
  - flake8 --exclude=migrations .
script:
  - docker run -t lop.io test
